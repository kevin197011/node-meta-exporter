global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: "node-meta-exporter"
    scrape_interval: 5m
    scrape_timeout: 30s
    static_configs:
      - targets: ["node-meta-exporter:9101"]

  # Example: use jumpserver_host_meta to relabel node_exporter targets
  # - job_name: "node-exporter"
  #   file_sd_configs:
  #     - files:
  #       - /etc/prometheus/targets/*.json
  #   metric_relabel_configs:
  #     - source_labels: [instance]
  #       target_label: __address__
